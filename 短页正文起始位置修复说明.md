# 短页正文起始位置修复说明

## 背景

这次处理的是阅读页分页后的正文垂直位置问题。普通正文页不按每页字数或内容高度做垂直居中，而是先把阅读页面扣除正文上下边距，得到一个正文可视区域，然后所有普通正文都从这个可视区域顶部开始排。

## Legado 的做法

Legado 的关键点不是“整屏居中”，而是“正文区顶对齐”：

- `AndroidLegado/app/src/main/java/io/legado/app/ui/book/read/page/provider/ChapterProvider.kt`
  - 读取 `paddingTop`、`paddingBottom`。
  - 用 `visibleHeight = viewHeight - paddingTop - paddingBottom` 得到正文可用高度。
  - 用 `visibleBottom = paddingTop + visibleHeight` 和 `visibleRect` 限定正文绘制区域。

- `AndroidLegado/app/src/main/java/io/legado/app/ui/book/read/page/entities/TextLine.kt`
  - 行坐标从 `ChapterProvider.paddingTop + durY` 开始。
  - 也就是说，正文天然从“上边距之后”开始，不会因为本页字数少就下移。

Legado 只在少数特殊场景做垂直居中，例如加载提示页、空章节标题、单图模式标题或图片等。普通章节正文页不套用这种居中逻辑。

## 本项目的实现

本项目原来的整体结构已经接近 Legado，因此这次没有改分页数据结构，也没有新增按内容高度计算的字段。实现重点是把“普通正文顶对齐”固定下来，避免后续样式或短页逻辑把正文页居中。

### 1. 正文上下边距继续由页面容器负责

文件：`app/src/main/java/com/metahumanz/pacilread/reader/modern/ui/ReaderStyleController.java`

阅读页的上下边距仍然加在 `page_current` / `page_incoming` 容器上：

```java
((ViewGroup) views.pageCurrent).setPadding(leftPadding, topPadding, rightPadding, bottomPadding);
((ViewGroup) views.pageIncoming).setPadding(leftPadding, topPadding, rightPadding, bottomPadding);
```

这里的 `topPadding` 和 `bottomPadding` 已经合并了：

- 用户设置的阅读上下边距。
- 系统状态栏 / 导航栏 inset。
- 顶部和底部 HUD 预留空间。

所以正文视图本身不再额外承担页面边距，避免边距被重复计算。

### 2. 分页高度继续使用“页面高度减上下边距”

文件：`app/src/main/java/com/metahumanz/pacilread/reader/modern/content/ReaderContentController.java`

分页时使用的正文高度来自 `getRegularReaderPageHeight()`：

```java
return Math.max(0, height
        - views.pageCurrent.getPaddingTop()
        - views.pageCurrent.getPaddingBottom());
```

这个逻辑和 Legado 的 `visibleHeight = viewHeight - paddingTop - paddingBottom` 对齐。也就是说，分页算法只关心正文可视区域高度，不拿整屏高度做计算。

### 3. 普通正文页绑定时不增加垂直偏移

文件：`app/src/main/java/com/metahumanz/pacilread/reader/modern/paging/ReaderNavigationController.java`

每次绑定正文页时，正文 `topMargin` 都明确重置为 `0`：

```java
paging.updateBodyTopMargin(bodyView, 0);
```

这保证了普通正文页不会因为短页、最后页、双页右页等情况被动态下推。

### 4. 正文 TextView 显式固定为顶部对齐

文件：`app/src/main/java/com/metahumanz/pacilread/reader/modern/ui/ReaderStyleController.java`

在 `stylePageBodyView(...)` 中，正文视图现在显式设置：

```java
textView.setGravity(Gravity.START | Gravity.TOP);
```

这样即使以后 XML、主题、样式或临时代码改动影响了 `TextView` 的默认 gravity，普通正文页也会稳定从正文区域顶部开始绘制。

### 5. 没有改动 PageSlice / ReaderPaginator 接口

这次没有给 `PageSlice` 增加 `contentHeight`、`verticalOffset`、`lineCount` 等字段，也没有让 `ReaderPaginator` 输出每页实际排版高度。

原因是当前需求已经明确为“完全跟 Legado”：普通正文页不做按字数或内容高度的垂直居中。保留原有接口可以避免影响：

- 阅读进度 offset。
- 选区坐标。
- 搜索高亮。
- TTS 高亮。
- 分页缓存签名。
- 翻页动画和双页模式。

## 行为结果

修复后的阅读页行为如下：

- 普通正文页首行始终从正文可视区域顶部开始。
- 调整上边距 / 下边距会改变分页可用高度，但不会让短页正文居中。
- 章节最后一页即使内容较少，也不会为了“看起来居中”而整体下移。
- 双页模式左右页使用同样的正文区计算；右页为空或短页时也不会额外下推。
- 加载提示、空内容提示这类非正文页以后仍可以单独做居中，但不能影响普通章节正文。


## 结论

这次改动的核心不是增加一套新的短页居中算法，而是把 Legado 的阅读排版原则固定下来：

> 正文区域由页面边距决定，普通正文从正文区域顶部开始排；短页不按字数、不按内容高度做整页垂直居中。

这样做能保证阅读时每一页的首行位置稳定，避免章节末页或短页出现明显跳动，同时也减少对分页、选区、TTS、高亮和翻页动画的连锁影响。
